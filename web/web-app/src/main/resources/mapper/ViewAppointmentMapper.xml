<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.app.mapper.ViewAppointmentMapper">
    <resultMap id="Appointment" type="com.atguigu.lease.web.app.vo.appointment.AppointmentItemVo" autoMapping="true">
        <id property="id" column="id"></id>
        <collection property="graphVoList" ofType="com.atguigu.lease.web.app.vo.graph.GraphVo" select="t1" column="apartment_id">

        </collection>
    </resultMap>

    <select id="getDetailByUserId" resultMap="Appointment">
        select va.id,
               va.apartment_id,
               va.appointment_time,
               va.appointment_status,
               ai.name apartment_name
        from view_appointment va
                 left join apartment_info ai on va.apartment_id = ai.id and ai.is_deleted = 0
        where va.is_deleted = 0
          and user_id = #{id}

    </select>
    <select id="t1" resultType="com.atguigu.lease.web.app.vo.graph.GraphVo">
        select url,
               name
        from graph_info
        where is_deleted = 0
          and item_type = 1
          and item_id = #{apartment_id}
    </select>
</mapper>
